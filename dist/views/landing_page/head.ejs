
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= nomeApp %> - Sistema de Agendamentos</title>
  <meta name="description"
      content="<%= nomeApp %> é um sistema que fornece uma reserva automatizada e agendamento online de seus serviços. Ele verifica a disponibilidade dos calendários e extrai horários livres. Você controla como os clientes podem reservar os horários abertos.">
  <link rel="icon" href="https://<%= dominio %>/icons/logo.png" type="image/x-icon">
  <link rel="short icon" href="https://<%= dominio %>/icons/logo.png" type="image/x-icon">
  <meta property="og:image" content="https://<%= dominio %>/icons/logo.png">
  <meta property="og:url" content="https://<%= dominio %>">
  <meta property="og:title" content="<%= nomeApp %> - Sistema de Agendamentos">
  <meta property="og:description"
      content="<%= nomeApp %> é um sistema que fornece uma reserva automatizada e agendamento online de seus serviços. Ele verifica a disponibilidade dos calendários e extrai horários livres. Você controla como os clientes podem reservar os horários abertos.">
  <meta property="og:site_name" content="<%= nomeApp %>">
  <meta property="og:locale" content="pt_BR">
  <meta property="og:type" content="website">
  <meta property="og:image:type" content="image/png">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">
  <meta property="og:image:alt" content="<%= nomeApp %> - Sistema de Agendamentos">
  <meta name="keywords" content="agendamento online, reserva automatizada, sistema de agendamento, <%= nomeApp %>">
  <meta name="author" content="AgilizaWeb">
  <script type="application/ld+json">
  {
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": "<%= nomeApp %>",
      "url": "https://<%= dominio %>",
      "potentialAction": {
          "@type": "SearchAction",
          "target": "https://<%= dominio %>/search?q={search_term_string}",
          "query-input": "required name=search_term_string"
      }
  }
  </script>

  <link href="./css/tailwind.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
      :root {
          --primary-color: #2563eb;
          --secondary-color: #1e40af;
          --accent-color: #3b82f6;
          --text-color: #1f2937;
          --light-text: #6b7280;
          --background: #f9fafb;
          --primary: #0F172A;
          --secondary: #1E293B;
          --accent: #3B82F6;
          --text: #1E293B;
          --light: #F8FAFC;
          --gray: #64748B;
      }
      body {
          font-family: 'Poppins', sans-serif;
      }

      .section {
          opacity: 0;
          transition: opacity 1.5s ease-in-out;
      }

      .section.visible {
          opacity: 1;
      }

      .hover-highlight:hover {
          background-color: #e2e8f0;
      }

      .scroll-to-top {
          position: fixed;
          bottom: 20px;
          right: 20px;
          color: white;
          width: 40px;
          height: 40px;
          border-radius: 50%;
          display: none;
          cursor: pointer;
      }

      .scroll-to-top.visible {
          display: flex;
          justify-content: center;
          align-items: center;
      }

      .mobile-menu {
          display: flex;
          flex-direction: column;
          align-items: center;
          background-color: rgba(0, 0, 0, 0.592);
          backdrop-filter: blur(10px);
          position: absolute;
          top: 100%;
          left: 0;
          width: 100%;
          height: 100vh;
          transform: translateY(-200%);
          transition: transform 1s ease;
          overflow: hidden;
          z-index: 999999;
          gap: 2px;
      }

      .mobile-menu.open {
          transform: translateY(0);
      }

      .mobile-menu a {
          padding: 10px;
          color: white;
          width: 100%;
          text-decoration: none;
          border: 2px solid gray;
          text-align: center;
      }

      .nav-link {
          position: relative;
          /* padding: 0.5rem 1rem; */
          /* color: var(--light-text); */
          transition: all 0.3s ease;
      }

      .nav-link::after {
          content: "";
          position: absolute;
          bottom: 0;
          left: 50%;
          width: 0;
          height: 2px;
          background: var(--primary-color);
          transition: all 0.3s ease;
          transform: translateX(-50%);
      }

      .nav-link:hover::after {
          width: 100%;
      }
  </style>
</head>